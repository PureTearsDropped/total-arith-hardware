# research ブランチ / research branch

> ⚠️ **ここは検証前の作業場。** `main` は監査・全緑のものだけ。ここのコードと主張は
> 未検証のまま置かれる。/ **Unverified work-in-progress.** `main` carries only
> audited, all-green work; anything here may be wrong.

## テーマ1: 「不明」を一級市民にする — 4値桁 {0, +1, −1, ?}

**動機（2026-07-19 の外部AI監査シリーズの帰結）**: CUDA/ハード実装で見つかったバグ6件は
全て同族 —— 「不明」が値でなく `(値, フラグ)` の合成表現の隅（表示0+GEビット・SUNK・
GE|LE）に住んでいるため、演算のたびに全部の隅を覚えていないと嘘をつく。
桁のアルファベット自体に **? （この桁の値は不明）** を持てば、この族は構成的に消える。

**既にある部品**（`explorations/` と `sed/`）:
- `quadsign.py` — 数レベルの2ビット4値状態（確定/大きさ不明/符号不明/型違い）。
  発見: MAX と MIN は向きが逆 ⟹ `MAX+(−MIN)` は確実に正（3値実装は諦めていた）
- `sed/trit_status.py` — 符号トリット{−,不明,+}の代数は gate9・**不明は吸収的**。
  「境界なしは4番目の状態が要るか」という問いは今回の GE|LE 問題を先取りしていた
- `bfp_sed._ival` — フラグと区間は**互いに優劣がつかない**表現
  （P1単項のSUNKで「大きさ≥x・符号不明」はフラグが勝ち、区間は (−∞,∞) に落ちる）

**開いている問い**:
1. 桁レベル ? の代数 — gate9 の4値拡張（? は吸収的）はどんなゲート数になるか
2. Avizienis 限定キャリー加算（幅無依存・深さ13）は ? 桁入りでも成立するか
3. 2ビット符号つき桁 {00=0, 01=+1, 10=−1, 11=?} で emit_sv の自動生成が通るか
4. 数レベルフラグ（現行）と桁レベル ? の変換規則・情報量の比較

## テーマ2: 三値のまま「不明」を桁に持たせる（`ternary_mark/`）

> 🚨 **一部撤回済み**（2026-08-25、外部レビュー）。carry 経由の相殺を取りこぼし、
> `LE + LE` で健全性が破れる反例が出た。詳細と次にやることは `ternary_mark/CORRECTION.md`。
> 以下の「加減乗除すべてで嘘 0」は**成立しない**。表現の方向性と「4 値化は要らないかも
> しれない」までは生きている。

**テーマ1 への別解**（2026-08-25）。`?` という 4 値目を作らずに同じ目的を果たす。
規則は一本 —— **どちらの端も、端のトリットが無限に続くと読む**。下へは幾何級数が収束して
誤差棒の端点になり（`0` なら厳密）、上へは発散して溢れになる（最上位が `0` なら溢れなし）。
桁は三値のままなので配線正規形を変えず、フラグ用ビットは 0 本。

相殺（`−1 + 1 = 0` で「打ち消した」と「誤差が無い」が区別できない問題）は、**予約桁を
1 つ確保して印を下ろす**ことで解決した。桁上げは上へ、相殺の印は下へ —— **逆向きの
二本のリップル**で、どちらも隣の桁としか話さない。小数点の位置を知る必要がない。

**テーマ1 の開いている問いへの答え**:
1. 桁レベル `?` の代数・gate9 の 4 値拡張 → **4 値化しなくてよい**。相殺検出は
   `(xp&yn)|(xn&yp)` = 3 ゲート/桁 + OR 木（値の経路の 17% 前後）
3. 2 ビット符号つき桁 `{00,01,10,11=?}` で emit_sv が通るか → `11` を使わないので
   **問いが消える**。既存の (p,n) レールのまま
（2 の Avizienis 限定キャリーと 4 の変換規則は未着手）

`explorations/add_price.py` の「加算は符号か締まりのどちらかを払う」に対しては、
**予約桁 1 つを払えば両方残る**（払う量は固定で、相殺が何回起きても累積しない）。

加減乗除すべてで嘘 0（各 3000〜6000 例）。**新しい桁規則を作ったのは加算の一回だけ**で、
減算は符号反転（配線）、乗算は同じリップルを足し込み段に、除算は候補として mul と sub の
上のプログラム。詳細と再現コマンドは `ternary_mark/README.md`。

**未検証**: 正規化との相互作用、合成していないこと（ゲート数は面積の代理にすぎない）、
乗算の印が保守的すぎる可能性。

## テーマ3: 初等関数 = 指数分解 + 係数テープの積和（`remez/`, 2026-09-02）

Newton の反復でも級数の打ち切りでもなく、**range reduction（指数バス）+ Remez 多項式の Estrin 積和 + 片側境界つき
出口** で exp / expm1 / log / sqrt / rsqrt / sin / cos を定義した。Remez はオフライン（mpmath）、機械に載るのは
係数テープ（K1: 合成時定数）と縮小の配線だけ。

- **テープ 16 本**（8 核 × f32/f64）。量子化後の誤差上界は **Fraction だけの証明**（`supnorm_exact`: 部分区間の Taylor
  模型・級数の尾は比判定で閉じる）。片側版は多項式 2 本でなく `±p·2^-e`（指数の付け替え = ゲート 0 + 加算 1 回）。
- **spec（整数だけ）** 7 関数 × lo/hi/near。**float32 幅を全入力総当たり**（spec そのもの・双子でない・42.8 億点 × 6）:
  7 関数すべて完了（exp/expm1 は両符号、他は正のみ・負は鏡または CPLX 経路、計 578 億回の照合）: **片側境界の違反 0・
  符号違反 0・厳密の嘘 0**。最悪の片側誤差 1.0005（exp/sin/cos）〜1.0039（rsqrt, 核精度 e=33）ulp、near 0.5001 ulp、
  いずれも主張の内側。最悪点は 7 関数とも「結果が 2 のべきの直下の細かい二進位に落ちる引数」で、縮小の方式
  （指数バス・偶奇テープ・Payne–Hanek）の差は見えない。掃引そのものの過程（二本並走の誤診）は `remez/README.md` §3。
- **ゲート版 exp/expm1**（指数バス込みで全部ゲート）: f32 = 270,592 ゲート・深さ 791、f64 = 687,173・深さ 863。
  f32/f64 とも 535 の敵対点で spec と bit 一致（フラグ込み）。
- **ゲート版 log/sqrt/rsqrt**（`remez/gate_logroot.py`, 同じ部品）: f32 = 338,660 / 387,652 / 471,319 ゲート
  （深さ 713 / 610 / 625）、f64 = 1,095,224 / 1,366,904 / 1,536,011（深さ 907 / 800 / 801）。f32 1461 点・f64 1485 点で
  9 出力すべて spec と bit 一致（フラグ込み・assert 0）。sqrt/rsqrt の 2 テープは木を 2 本並べず**係数を h で選ぶ**。

**外れた予想・訂正**（`remez/README.md` §4.2 に順を追って）:
1. 値を SD で持ち回ると f32 686k・深さ 1011。**SD は加算器の中だけ**にして 271k・791（SD の存在意義は「桁上げが
   隣で止まる」で、それは和を取る瞬間にしか要らない）。
2. hi の ulp 主張が締めすぎ（lo の式を流用）。総当たりが expm1 で 14 件、log（最初の実行）で 3 件の「主張超え」を
   挙げて式の誤りを見つけた。値の誤りは 0。訂正後は記録した sup で再判定（`sweep_claims_check.py`、再掃引なし）。
3. expm1 の (v·2^k) − 1 を位置 0 の窓で引くと f64 で 278k（全体の 30%）。k を錨にした窓で 23k。**その窓のバグ 2 件は
   535 点の差分テストを通り、境界値固定の単体テストが見つけた** → 規律 R4（ゲート側だけの境界には単体テスト）・
   R5（sticky の前提「落ちた量 < 1 単位」を数える）を GATE_CONDITIONS.md に追加。
4. Wallace の終端: 高さ 3 の列の圧縮が隣を 3 にして層が幅だけ続く（実質リップル）。高さ ≤ 3 で止め HA 層で閉じる。
5. **窓つき加算の固定 G=4 は根のテープでは足りない**（log/sqrt/rsqrt のゲート版で。値は全点一致しつつ監視 assert が
   97 件）。Estrin の節点の値は t の多項式で、**根が定義域の内側にあれば相殺は非有界**。全テープを調べると exp/log/
   sin/cos は根が外（最大相殺 < 1 bit → G=4 に根拠がついた）、sqrt_p1/rsqrt_p1 は根が内側（19〜25 bit）、rsqrt_p0 は
   根が無くても f64 で 4.4 bit > G。直し = 窓を下へ W+extra+3−G 桁伸ばす（落ちなければ厳密・落ちるのは先頭差 ≥ 2 の
   ときだけ）。代金 f32 +25〜33%、f64 +23〜25%。監視も「落とした ∧ 先頭 < W」に正確化（旧条件は偽陽性があり得た）。
6. p1 テープ（t ∈ [−½, 0)）の係数は Wk より広い（f64 rsqrt 76 ビット）。spec が厳密に使う定数なので幅を落とさない（K1）。

**未着手**: sin / cos のゲート版、Estrin 節点ごとの正規化をやめて固定小数点で通す形、合成。
sin/cos の f64 縮小（Payne–Hanek の fbits=62）は **文献値の引用**で総当たりしていない。

### 続き: 二次代数の還元（`../total-arith-cuda/hyper_quad.py`, 2026-09-02）

超複素（Cayley–Dickson, M = 2^k）の一変数関数は行列関数 f(L_x) でなく **v² = −|v|²·e0** で
f(a+v) = Re f(a+i|v|) + (v/|v|)·Im f(a+i|v|) — rsqrt 1 回 + スカラー関数 3〜4 本 + 乗算 (3M+2) 回。
テーマ3 の spec を backend にして旗ごと通る。行列経路と 1e-15 で一致・恒等式 7 本・連続性・全域性は検算済（両 backend PASS）。
コストは GATE_CONDITIONS.md の表（rsqrt の実測 1.5 M を入れると M=2 は行列が 2 割安く、M=4 で ×5.7、M=16 で ×270）。得られたもの: **零因子の log は存在する**（L_z 特異は表現の
性質、ℝ[z] ≅ ℂ の中で exp(log z) = z が行列 expm でも成立）・**順序旗は非単調関数（sin/cos）の不確かな引数を通ると
符号まで失う**（exp/cosh の還元は全成分 111 — 幅を運ぶのは四値/区間層の仕事）。未着手: atan テープ（log/pow の虚部が
float64 atan2 のまま、旗 11 で明示）。

## 運用

- 検証が通ったものだけ `main` に昇格（オラクル+回帰つきで）
- ここの実験は消えることがある

## 実験0（2026-07-19・済）: 半分選び定理

利用者の仮説「先頭+1のとき下の桁の−1はその桁の半分を表す」→ **成立**（`four_value/bisection.py`・全列挙違反0）。
- 先頭+1のSD語のMSB読み = 区間二分（−1=下半分/0=中央半分/+1=上半分）。冗長性=中央の重なり。
- **?を末尾に並べた語 ≡ 連続区間**（幅 2·2^m−1）。孤立?は隙間を作る。
- ⟹ **(桁前置き, ?深さ) = 値+誤差棒 = 有効数字算術**。数レベルフラグ(GE/LE)は「?開始位置」の粗い射影。
- 設計指針: **?は末尾正規形**を保つ。
- 次=実験1: Avizienis加算は?末尾正規形を保存するか・?深さの成長は≤1桁か（floatのulpと同型か）。

## 実験1（2026-07-19・済）: 加算は?末尾を保つ + 可能性つきキャリー

`four_value/experiment1_add.py`（全列挙照合）:
- **?末尾正規形は加算で保存**（途中の穴 0/400）。深さ成長は ほぼ+1・時々+2（境界の先読みが?領域を覗く分）。
- 4値化の幅の損 中央値2.03×＝区間を?語で包む固有コスト（理論下限≈2×と一致）。
- **可能性つきキャリー（利用者の案）**: キャリー線も可能性集合を運ぶ・完全局所計算 →
  健全（含み損ね0/3830）・真集合と98.1%ぴったり・余分0.019個/桁。
  ⟹ **不確かさは既存のキャリー機構に乗って流れる。?の特別扱い不要＝配線=計算の精神そのまま**。
- 次: キャリー線のアルファベット（集合{0,1}/{−1,0}等）の最小ビット数・ゲート化(gate9拡張)・乗算への拡張。

## 実験2（2026-07-19・済）: ノルム多項式の網（±2±5）

`four_value/experiment2_normfilter.py`。外部AIの提案(±2±5の「2乗の魔法」)を検証・位置づけ:
- 正体は古典の**レゾルベント/ノルム形式**(新数学でない)。倍化合成 P(x−a)·P(x+a) で符号を列挙せず係数が決まる。
- 実測: n=5(次数32)まで偽陽性0・幻影(±1,±5)は残差で棄却・スープ濾過で真集合復元。
- **限界の発見**: ?×m桁の根=ちょうど連続区間 ⟹ **?尻尾には多項式は冗長**。輝くのは疎な符号不明(真集合に隙間)のときだけ。コストは台帳側の次数2^nに移るだけで消えない。
- **台帳の分業法則**: 密な切り捨て=区間(O(1)厳密) / 疎な符号不明=ノルム多項式(2^n・唯一の厳密) / フラグ=両者の影。パターン則の地図(密=保守則最適/疎=情報回収)と同型。
- 部品の対応: SUNK≡x²−v²・?桁≡x³−x・倍化合成=畳み込み(配線)・評価=powersum。

## 実験3（2026-07-19・済）: 符号フォークと上位確定（利用者の2案）

`four_value/experiment3_fork_commit.py`:
- **A 符号フォーク（±2桁下げペイロード）**: 位kの±1分岐が変える出力桁は**最大3桁の窓**（大半1桁）・分岐差=常に2·2^k・遠い不明2個は2×2局所フォークに完全分解(1500/1500) ⟹ **2^nは局所性で消える**（窓が重ならない限り線形）。
- **B 上位確定の下方伝播（MSB先行正規化）**: 窓（重なりあり）に収まる限り上から確定→再センタリング→下へ。健全0違反・スラック中央1.85×・**接頭辞+?尻尾の正規形を自動生成**。隙間つき{−v,+v}では停止=Aの出番。
- **3本柱の分業**（全て利用者発案）: 区間型→B / 符号の隙間→A（局所×2） / 置き場=?尻尾。重なる多数の符号不明のみ実験2のノルム多項式へ。
- 次: この分業を1つの加算器に統合（フォークトークン+?尻尾の混在語）→ゲート化→深さ実測。
