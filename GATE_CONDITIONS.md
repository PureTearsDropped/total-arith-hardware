＜⚠️ 生成AI使用・要検証＞

# ゲート回路の条件表 — (U,V,W) 双線形ユニットを 論理ゲートで 組む前の 設計契約

`c = W·( (U·a) ⊙ (V·b) )`（前線形 → R 個の並列乗算 → 後線形）を **ゲートから** 組み直す。
組む前に、これまでの全域算術で 確立した規則を **「入力→出力の契約」** として 洗い出す。
各条件は 後で ゲートが 実装すべき 振る舞い。**未確定の分岐は 【要確定】** と記す。

出所: `bfp_sed.py`（代数コア）, `sd2_core.py`, `sd2_gates.py`, `saturating_arith.py`, `SPEC.md`。

---

## A. 数の表現（1 本の線が 何を運ぶか）

- **A1 符号つき桁** `t ∈ {−1,0,+1}` を 2 本 `(p,n)` で: `enc(t)=(t==1, t==−1)`, `dec(p,n)=p−n`。
  冗長ゼロ `(1,1) ≡ (0,0) = 0`（同じ値の複数表現・値は厳密）。
- **A2 ブロック浮動** 成分値 = W 桁の 仮数 × `2^E`。**E は ブロックの M 成分で 共有**（配線シフトで整列）。
- **A3 予約レール** `±MAX`（上に飽和・全桁）／`±MIN`（最小の非零・最下位1桁）。
  別記号でなく、**フラグが 予約の意味を 与える** 通常のビット列。
- **A4 NaN/Inf のビット列は 存在しない**（全域化の不変条件）。

## B. 各成分に 付く 状態（フラグ）

- **B1 以上** ge（bit0, ≥）: `|真| ≥ |表示|`。
- **B2 以下** le（bit1, ≤）: `|真| ≤ |表示|`。
- **B3 組** (ge,le): `00=厳密` / `10=≥` / `01=≤` / `11=境界なし（不明）`。
- **B4 符号不明** sunk: 表示の **符号が 保証されない**（別ビット）。

## C. 全域化の規則（回路が 必ず 実装・NaN/Inf は 決して 出さない）

- **C1** `x × 0 = 0`（厳密）。［部分積が 全部 0 で 自動］
- **C2** `x ^ 0 = 1`（空積）。
- **C3** `a / 0` = **【要確定・分岐】**
  - (i) **Moore–Penrose `a/0 = 0`**（代数コア `bfp_sed`）＝ 符号を 捏造しない。**推奨（原理的ユニット）**。
  - (ii) **飽和 `a/0 = sign(a)·MAX`, `0/0 = 0`**（`saturating_arith`）＝ 大きさを 残すが 符号を 作る。
  セデニオンでは `N(b)=Σbᵢ²=0 ⟺ b=0` なので 判定は N の 零検出 1 個。
- **C4 溢れ**（E が Emax 超 / 仮数が Emax で W 桁超）→ **±MAX に飽和・ge を立てる・符号は保つ**。
- **C5 潰れ**（非零仮数が MIN より下へ 完全に落ちる）→ **±MIN・le を立てる・符号は保つ**。
- **C6 全演算が [−MAX,+MAX] に飽和**。除算器だけでなく **乗算器も** 飽和させる
  （`saturating_arith` の教訓: `…×MAX` が 次の積で 即 範囲外＝Inf。だから 全演算で 飽和）。

## D. 回路が 保つべき 区別

- **D1 二種の 0**: 真の0（全桁0・厳密な相殺 or 零因子）→ `(ge,le)=00, 仮数0`。
  潰れた0（アンダーフロー）→ `±MIN, le=1`。**混同しない**（別出力）。

  **概念（利用者）: ±MIN は「潰れた0」でなく、有限機械における 符号つき無限小 ε。**
  古典解析の「0 に限りなく近いが 0 でない・向きを持つ量（dx）」を、無限に小さくできない機械が
  **床 MIN で止めたもの**。`le`（≤）が これを 忠実にする: `±MIN・le` = 「|x| ≤ MIN・符号既知」
  ＝ **無限小の帯 (0, MIN] に 向き付き**（0⁺ か 0⁻）。特定の小さい値でなく「そちら向きの無限小」。
  - **符号捏造が消える理由そのもの**: `1/(本当の0)=0`（中立・向きなし・Moore–Penrose）／
    `1/(0⁺)=+大(→+MAX)`, `1/(0⁻)=−大`（ε は 向きを持つ ⟹ 方向極限が 正しく出る）。
    IEEE は 0 を 常に 0⁺ 扱いして `1/0=+inf` と **向きを捏造**する。**0 と ε(±) を分けた瞬間、
    古典の方向極限が 戻る。** ＝ 二種の0を 区別する 本当の御利益。
  - **有限の限界**: 機械 ε は **一段だけ**。真の無限小の塔（ε ≻ ε² ≻ …・ε²=0 の双対数）は 持てない
    （MIN の下は 床＝何もない ⟹ `MIN×MIN` は 0 か MIN に 落ちる）。
    ＝ **有限性で 一段に 切り詰められた 無限小**。
- **D2 符号の保存**: 飽和・潰れの値は 飽和前の値の 符号を 保つ。
  **例外**: 相殺する 符号つき和で 符号が 決まらない → `sunk=1`。

## E. 演算ごとの 契約

- **E1 乗算**（R セルの 各々）:
  - 厳密入力: フラグは 正規化からのみ（切り捨て→ge / 溢れ→ge / 潰れ→le）。**積は sunk を 立てない**。
  - フラグ付き入力: 出力成分ごとに 符号つき区間を 伝播。単調→ge/le、相殺→境界なし＋（区間が0跨ぎなら）sunk。
- **E2 加減**（U,V,W の 線形層）: **E を整列**（小E側を 左シフト＝無損失）→ 桁ごと 3:2 圧縮。
  同じレールで 飽和した 異符号成分が 相殺するとき **sunk を立てる**（符号不明の 唯一の発生源）。
- **E3 除算**（ユニットが 割るなら）: `a·conj(b) / N(b)`。
  スカラー除数→締まる `(≥)/(≤)→≥`。セデニオン除数→依存性で 保守的（境界なし）。
  `N=0`→C3。除数区間が 0 跨ぎ→**境界なし＋sunk、値は 0 でない**。

## F. 型・次元・指数（型違い）

- **F1 次元 M**: 両オペランドは 同じ M（配線 OMEGA が M 依存）。異なる M → **型エラー**、
  または CD 塔で 埋め込み（新しい虚部を 0 詰め）。
- **F2 捻れ**: **型 = (M, OMEGA/群, E)**。同じ M でも 捻れが違えば（行列 vs 除算代数）**意味的型不一致**。
- **F3 指数 E**: 加減は E 整列、乗算は E 加算。ブロック共有Eの 維持は 正規化の 責務。
- **F4 (U,V,W) の形**: `U:R×m, V:R×n, W:p×R`。`a:m, b:n, c:p`。`U·a` と `V·b` は 共に 長さ R
  （⊙ の 前提）。長さ不一致 → 型エラー。

## G. 零因子・±MIN の 注意（セデニオン M≥16）

- **G1 零因子** `a≠0, b≠0` で `a·b=0` が 在る → 真の0（D1）。正しく 潰れと 区別。
- **G2 ±MIN 規約の 破れ**: 予約語 ±MIN は `a·b=0 ⟹ a=0 or b=0` を 仮定するが、これは **M≥16 で破れる**。
  ⟹ 回路は「MIN×非零 ≠ 0」を **仮定してはならない**（セデニオン積は ±MIN でも 厳密0 になりうる）。
  直しは `L(x)` の 零空間（【要検討】）。

## H. 組み上げた ユニットの 振る舞い（3 層）

- **H1 前線形 `U·a, V·b`**: 符号つき整数の 線形結合（Strassen は 成分 {−1,0,+1} ⟹ **加減だけ・乗算器なし**）。
  各々 E2 に従う（整列・相殺で sunk・C4/C5 飽和）。
- **H2 R 個の 要素ごと積**: 各々 E1。
- **H3 後線形 `W·(·)`**: E2。
- **H4 フラグの流れ**: 3 層を 貫いて 伝播（前和が 溢れ ge → その積が 継承、後和が 区間規則で 合成）。

---

## 組み上げ順（小さいゲートから・まだ書かない・確認用）

1. **原始ゲート**: `enc/dec (p,n)`, `AND/OR/NOT/XOR`（A1）
2. **gate9**: 符号つき桁 1×1 の 積 `t = x·y`（E1 の 芯）
3. **全加算器 → 3:2 圧縮器（18 ゲート）**: 値保存 `low+2·high=x+y+c`、冗長（C・D1 の芯）
4. **符号つき加算木 ＋ 指数整列シフタ**: U,V,W の 線形層（E2, F3）
5. **正規化器**: 切り捨て→ge / 溢れ→±MAX・ge / 潰れ→±MIN・le / 真0 vs 潰れ0（C4,C5,D1）
6. **乗算セル**: 部分積(gate9) → 圧縮木 → 正規化 → フラグ（E1）
7. **零検出 ＋ 除算**（要るなら）: N の零検出→C3、区間→E3
8. **フラグ伝播論理**: 区間 → ge/le/sunk（B, E1, E2）
9. **組み立て**: U 層 → V 層 → R 乗算セル（並列）→ W 層 ＝ (U,V,W) ユニット（H）

**確認事項（組む前に）**: ① C3 の 除算規約（(i) a/0=0 推奨 か (ii) 飽和±MAX か）。
② この 9 段の順で 良いか。③ 抜けている 条件は ないか。
【確定済】① = (i) a/0=0（利用者承認）。②③ 承認済み。

---

# 到達点（実装結果・全て 検証済み 0違反）

## 実装ファイルの 地図

```
土台（ゲートから）        gate_bilinear.py  段1〜7b: 原始ゲート→gate9→3:2圧縮器(18ゲート)
                                          →多桁加減→乗算セル→(U,V,W)組立→canonicalize
                                          →飽和±MAX/±MIN=ε・二種の0・フラグ
ブロック浮動（底2）       gate_bfp.py       B1〜B5: 表現(仮数×2^E)→積→和(指数整列)
                                          →ブロック正規化(共有E・ε)→(U,V,W)ユニット
配線の動物園             wiring_zoo.py     捻れ群代数の 枠（巡回/ブール立方/CD/symplectic/直積）
配線パターン             wiring_patterns.py Karatsuba(3積)・Gauss複素(3積)
多層回路                newton_recip.py    Newton逆数=固定多層配線・近似商+ge境界
レジストリ（束ね）        wiring_registry.py 19パターン・run(名前,a,b)・(U,V,W)正規化
表現論のレンズ           representation_lens.py D4/Q8/S3 の Wedderburn 分解 実測
```

## 中心的な 到達点

1. **(U,V,W) = 双線形アルゴリズム**（テンソルの rank-R 分解）に 全てが 正規化される:
   群積は U,V=選択・R=M²・W=群経路の 特別な場合。Strassen/Karatsuba/Gauss は R<素朴 の 削減。
   **積み重ねると 算術回路**（Newton 除算・rsqrt）**＝ 乗算型NN と 同型**（アテンション QKᵀ は 双線形層）。
2. **底2 ブロック浮動 (U,V,W) ユニットが 端から端まで ゲートで 動く**（gate_bfp B5）:
   W大→厳密(EQ)・W小→健全に丸め（嘘なし）・二種の0（真0=EQ 符号なし／ε=±MIN le 向き保持）・飽和 ±MAX。
3. **配線レジストリ 19 パターン**: 除算代数(complex/quaternion/sedenion)・幾何代数(Cl2/Cl3)・
   行列積(2×2/3×3/4×4, Strassen7, Strassen⊗Strassen 49)・畳み込み(巡回/負巡回)・
   Karatsuba/Gauss・非可換群(D4)・多層(Newton recip/rsqrt)。**negacyclic2 == complex**（独立突き合わせ）。
4. **表現論のレンズ**（Wedderburn・全て 0違反）: D4→R⁴⊕M₂／**Q8→R⁴⊕H（四元数は 群配線の 1ブロック）**／
   S3→R²⊕M₂。**一つの 非可換群配線 = 複数の 積の 同時実行**（表現論の SIMD）＝ Cohn–Umans の 入り口。

## サブエージェント監査（2026-07-18・2並列独立）の 結果と 契約

**監査課題**: 「普通の演算命令（値依存の Python 算術/比較/分岐）が ゲート/配線に 紛れていないか」。

- **gate_bilinear.py: 違反 0 件（データパス純度 100%）**。dec/enc は 境界のみ・sd_sum の 分岐は
  全て 形状依存（HDL の generate 相当）・飽和/量子化も 全マスクゲート。
- **gate_bfp.py + newton_recip_gates: 仮数パス=本物 ／ 指数パス=ホスト計算（違反 多数）**:
  E の加算/比較/min・シフト量計算・leading_pos（dec で値を覗く）・Newton 種（bit_length+可変シフト）・
  データ依存の 配線幅。「端から端までゲート」は 指数パスで 不成立だった（→ 下記 ゲート化で 修正）。

**修正（監査の 処方箋どおり・全て 差分テストで ホスト版と 一致）**:
- `gate_exponent.py` — 指数バス部品（2の補数・EW固定幅）: bus_add/sub/lt/max/min/clamp0（0/24000）、
  **優先エンコーダ**（nz の OR 累積・0/3000・冗長入力の罠も 明示テスト）、**バレルシフタ**（右=落とし桁
  非零を 段ごとに 収集・左=固定幅・0/3000。18桁 1 回 ≈ 792 ゲート＝従来 無計上だった 分）。
- `gate_bfp2.py` — フルゲート版 BFP: bfg_mul（指数 bus_add・0/1500）、bfg_add（min/sub/左バレル・
  0/1000）、**block_normalize_g**（先頭検出・シフト量・Emax 判定・全制御が 信号・0/600 ブロック、
  ε も 溢れ経路も 通過）。M=4/24桁/W=6 の 正規化 1 回 = **7,850 ゲート**（指数制御 込みの 初の 正直な数字）。
- `newton_recip.py::newton_recip_gates_full` — **種も ゲート**: 種 y=2^(F−1−L) は 優先エンコーダの
  one-hot を **位置反転した 純配線**（bit_length＋可変シフトが 配線に 消える）。幅 Wb 固定。
  整数版と 0/32 一致（b=1・2^19+7 等の 端 含む）。

**明文化する 契約（監査で 発見された 暗黙の前提）**:
- **K1 係数は 合成時定数**: `lincomb` の U,V,W 係数・`quantize` の sh は 回路の 構造定数。
  **信号値を 渡してはならない**（渡した瞬間 隠れ比較器/バレルシフタになり ゲート勘定が 嘘になる）。
  実行時に 変えたいなら 明示の mux/バレルシフタ（gate_exponent.py）を 使う。
- **K2 幅は 最悪ケースで 固定**: 信号の 値で 配線幅・ゲート個数が 変わってはならない（ZERO 詰め）。
- **K3 ゲート数の 注記**: st は 未最適化ネットリスト（定数入力ゲート含む）＝ 上限の 目安。

## ゲート実装の 三つの 規律（バグから 確立）

- **R1 冗長表現の罠**: 符号つき桁は 冗長 ⟹ 値の大小に 関わる 操作（溢れ判定・floor・±MIN判定）の
  **前に 必ず canonicalize**（正の非冗長形へ）。積和・付け替えは 冗長のまま で良い。
  （実例: 値1の冗長形が 高位桁を持ち 溢れ誤認／ゲート版 Newton の floor が +1 ズレ）
- **R2 方向づけ丸め**: 「打ち切り = 境界(ge/le)」は 自動でない。**単調性に 合わせて 各丸めの向きを 設計**
  して 初めて 健全（rsqrt: 内側 y² は ceil・外側 floor ⟹ 下界 0/80000。floor 一律だと 65601/100000 破れ）。
- **R3 底2の 付け替え**: 仮数の底 = 指数の底 = 2 ⟹ E の変更 = 桁位置の 再ラベル ＝ 無損失。
  ブロック正規化 = 一度解決 → 先頭位置 → 付け替え → E調整（大きさ判定は ここ だけ・核の演算には 不要）。

## 深さ削減（逐次性監査の 処方箋 実装・gate_fast.py・全て 差分テストで 同値）

**深さは 主張でなく 実測**（影の評価器 B: 各線が 深さを 運び、ゲートで max+1）。

| 回路 | 遅い版 | 速い版 | 手法 |
|---|---|---|---|
| sd_add2（SD 2数加算） | O(n) リップル | **13 段・幅 8/16/32/64 で 不変** | Avizienis 限定キャリー（隣接窓） |
| sd_sum（8行） | 301 | **33** | 層別 Wallace + sd_add2 |
| multiply（10桁） | 3411 | **70** | 同上（49 倍） |
| canonicalize（21桁） | 53 | **17** | 双方向 並列減算（Kogge–Stone）+ 符号 mux |
| priority_encoder（24桁） | 37 | **12** | suffix-OR 倍化木 + OR 木符号化 |
| bfg_add | 123 | **33** | 双方向差 並列 + prefix + 2行 sd_add2 |
| block_normalize_g | 269 | **123** | 上記全部 + Lmax トーナメント + 指数 prefix |

**回収した 報酬**: sd_add2 の「幅に 依らず 13 段」が 符号つき桁（冗長）表現の 存在意義 そのもの
（キャリー伝播が 隣 1 桁で 止まる）。従来は 毎回 リップルで これを 捨てていた（監査の 指摘どおり）。
**残る 逐次**（意味的・縮まない）: Newton の k 段（データ依存 再帰）・バレルの log 段・
浮動加算の 比較→整列→加算 列・冗長解決の Ω(log n) 下限。

## 第3回監査（multi_add.py / mul_fused.py / gate_fast.py 部品群）: 違反 0 件・合格

- **信号値が Python の 算術/比較/分岐に 落ちる箇所: 皆無**（grep 全数確認）。「値による選択」は 全て mux
  （ゲート）、「構造による選択」は 全て 形状・設計定数（契約 K1）。
- `group_component` の `OM[i][j]<0` = cd_omega 由来の 合成時配線表 ⟹ K1 適合（帰結は neg = ゲート0の 配線）。
- `bfg_sum_many` の `[:Dmax_bits]` = Emin 構成から 差≥0 が 保証 ⟹ clamp0 省略 正当（設計契約は bfg_add と 同型）。
- sd_add2 の 隣接参照・sd_sum_fast の while・Kogge–Stone の 倍化 = 全て 形状/合成時カウンタ ⟹ (b) 配線。
- canonicalize_fast が「値で 場合分けしない」（双方向 並列計算 + sign ワイヤ mux）の 模範形 と 評価。

⟹ **蓄積型 加算器・BFP 多数同時加算（丸め1回）・融合乗算器（積 解決なし・群積成分=融合MAC）まで
   全て「AND/OR/NOT/XOR + 配線」で 構成されている（独立監査 3 回 通過）。**

## SystemVerilog 実装（Icarus Verilog + cocotb・Python golden と 突き合わせ・全 PASS）

- 手書き 構造記述 `rtl/sd_primitives.sv`: gate9(16/16 悉皆)・compress3(64/64 悉皆+値保存)・
  sd_add2 N=16(506 ケース)。連続代入と generate のみ（振る舞い +/if 不使用）。
- **自動生成 `rtl/emit_sv.py`**: 監査済み Python 関数を トレーサで 走らせ **同一ゲートグラフを SV に**
  （手写しゼロ・転記ミス 原理的に 不可能）:
  sd_mult10(11,604 ゲート・304)・pe24(207・304)・barrel18(792・400)・
  blocknorm(10,971・120 ブロック=仮数/指数/フラグ ge/le/ε 一致)・
  **sed_comp(48,222 ゲート・セデニオン積成分=16積の融合MAC・60 ケース ref_mult 一致)**。
- 検証構図: cocotb が SV シミュレーションを **Python 実装（監査3回合格）を golden として import** し 照合。
  環境: Icarus Verilog 12.0 + cocotb 2.0.1（Python 3.14 は 公式外の 強制インストールで 動作）。
  Verilator 5.032(Debian) は cocotb 2.0 の 要求(≥5.036) 未満で 不使用（Icarus に 切替・検証の質は 同一）。

## FPGA 実装（openXC7 完全オープンフロー・Vivado 不使用）

**経緯**: Vivado 2026.1 は 無償枠が Windows 限定に 制度変更（Linux は 有償）→ 61GB 削除し
openXC7（yosys 0.62 + nextpnr-xilinx 0.8.2 + prjxray）を ソースビルド（タグ名 yosys-0.62→v0.62 修正・
AMD 出荷スクリプトの CRLF バグも 発見/修正）。

**面積 実測（yosys synth_xilinx・Artix-7）**:
| モジュール | LUT | A7-100T 占有 |
|---|---|---|
| sd_add2 N=16 | 256 | 0.4% |
| sd_mult10 | 3,717 | 5.9% |
| blocknorm（フラグ込） | 8,160 | 12.9% |
| blocknorm フラグ除去 | 7,488 | 11.8% |
| sed_comp（融合MAC） | 21,438 | 33.8%（**余裕で搭載可**） |
| top_arty（UART+sd_add2） | 786 | 1.2% |

**フラグのコスト（主張→測定で 訂正）**: 差分 672 LUT = **正規化ユニットの 8.2%・演算パイプ全体の ≈2.3%**
（「1%未満」は 甘かった。それでも ECC の 12.5% より 安い）。

**配置配線（nextpnr）**: top_arty 395 SLICE_LUT・**Fmax 244.86 MHz**（100MHz 制約に 2.4 倍余裕・
クリティカルパスは UART 側 = 定数深さ加算器は ボトルネックでない）。

**ビットストリーム**: fasm2frames + xc7frames2bit → **top_arty.bit（3.7MB）生成済み**。
残るは ボード到着 → openFPGALoader 書き込み → host_test.py 実機照合 のみ。
物理配線 vs 論理配線の 対応も 整理: U,V,W を LUT 焼き込み（差替=再合成）か 表駆動（差替=メモリ書込）は
設計選択（3層トレードオフの 物理版）。

## 初等関数 = 縮小 + 係数テープ + 出口（`remez/`・research・exp/expm1/log/sqrt/rsqrt のゲート版まで）

Remez 係数テープ（K1: 係数は合成時定数・定数×信号は「立っているビットの行」= 配線）を Estrin で積和し、
縮小（指数バス）と出口（片側境界 + 丸め）を含めて **exp / expm1 を端から端までゲート**で組んだ。
spec（`funcs_spec.py`, Python 整数だけ）と f32・f64 で 535 の敵対点すべて bit 一致（フラグ込み）。
**log / sqrt / rsqrt**（`gate_logroot.py`）も同じ部品で端から端まで組み、f32 1461 点・f64 1485 点で 9 出力すべて
spec と bit 一致（フラグ込み・ホスト assert 0）。詳細は `remez/README.md`。

**部品の深さ（実測・入力深さ 0 から）と数**:

| 部品 | f32（Wk=40） | 深さ | f64（Wk=64） | 深さ | 手法 |
|---|---|---|---|---|---|
| canonicalize（46 / 70 桁） | 1,958 | **18** | 3,200 | **20** | 双方向 Kogge–Stone + 符号 mux（gate_fast と同型・幅で 17→18/20） |
| 先頭位置 pe_bits（40 / 64） | 350 | **13** | 634 | **13** | suffix-OR 倍化木 + OR 木符号化 |
| 右バレル 窓つき（80→40 / 128→64） | 1,467 | **15** | 2,664 | **16** | MSB-first・落とした桁の非零を段ごとに収集 |
| 二進 Wallace 信号×信号 | 10,422 | **43** | 26,152 | **46** | AND 配列 → 3:2 層（高さ ≤ 3 で停止 + HA 層）→ K-S |
| 二進 Wallace 定数×信号 | 4,732 | 35 | 11,672 | 42 | 定数の立つビットの行だけ（配線）→ 同上 |
| 正規化 norm_mag | 2,612 | 34 | 3,998 | 34 | pe + バレル + 指数バス加算 |
| 窓つき BF 加算（G=4, sticky） | 8,240 | **91** | 13,635 | **96** | 指数比較 → 整列バレル → SD 圧縮 + sd_add2 → canonicalize → 正規化 |
| 窓つき BF 加算（G=4 **+ Dext**, 根のテープ用） | 15,829 | 96 | 27,428 | 103 | 同上、窓を Dext = W+extra+3−G 桁下へ（f32 44 / f64 75）。W+2W ビットの節点では 13,875→22,094 / 23,275→36,548 |
| 出口 finish（near） | 2,418 | 74 | 4,415 | 79 | ∓P·2^-e（定数シフト + 二進加減算）→ pe → バレル → 丸め → 溢れ/潰れ mux |
| **exp/expm1 全経路（6 出力）** | **270,592**（raw 327,110） | **791** | **687,173**（raw 795,456） | **863** | reduce 167/189 → Estrin 4 段 +538/+586 → finish +86/+88 |
| **log 全経路（3 出力）** | **338,660** | **713** | **1,095,224** | **907** | reduce 1,980+1,855 / 4,637+4,071 → Estrin（次数 13 / 22）298,597 / 1,012,577 → 尾 Ex·ln2 + u·q 25,276 / 54,466 → finish ×3 |
| **sqrt 全経路（3 出力）** | **387,652** | **610** | **1,366,904** | **800** | reduce +1,796 / +4,012 → Estrin（係数 mux, 次数 12 / 21, Dext 窓）372,960 / 1,338,962 → 指数 ± Ex/2（配線）→ finish ×3 |
| **rsqrt 全経路（3 出力）** | **471,319** | **625** | **1,536,011** | **801** | 同上（次数 14 / 23）456,666 / 1,508,092 |
| log+sqrt+rsqrt 合計（9 出力・縮小共有） | 1,193,671（raw 1,430,505） | — | 3,988,865（raw 4,542,803） | — | 共有は canonicalize + 正規化 1,980 / 4,637 だけ |

- 深さ O(log n) は **Estrin の木の高さ**（⌈log₂(n+1)⌉ = 4）についての主張。1 段の深さ ≈ 積 43 + 窓つき加算 91 は
  幅 Wk の log で伸びる（K-S・Wallace・倍化木）。数では **加算 > 乗算**（f32: 窓つき加算 9 回 114k、信号積 7 回 74k、
  定数積 5 回 23k）: 浮動の和は毎回 canonicalize・先頭位置・バレル・正規化を払う。
- K2（幅は最悪ケースで固定）: expm1 の (v·2^k) − 1 は位置 0 を底にした窓だと f64 で 1548 桁になる（278k ゲート）。
  **k を錨にした窓**（幅 Wk+6・one-hot デコーダ 2 行 + バレル 1 回 + sticky 1 桁）で 23k。幅は k の値でなく kmax で決まる
  のは同じで、窓の位置を信号で動かした（バレル = 明示のシフタ、K1 適合）。
- K3（st は未最適化の上界）: raw と fold（定数折り畳み後）を両方数えた。差は f32 17%・f64 14%。
- **log/sqrt/rsqrt で分かったこと**（`gate_logroot.py`）:
  - sqrt/rsqrt の 2 本のテープ（E の偶奇 h で p0/p1）は 木を 2 本並べず **係数を h で選ぶ**: 合成時定数ビットの mux は
    配線（等しいビットは定数、違うビットは h / ¬h）。代わりに葉の積が 定数積 → 信号積（Wallace）になる。
  - **p1 テープの係数は Wk より広い**（t ∈ [−½, 0) の単項式基底で |c_k| > 1: f32 rsqrt 45 / f64 sqrt 69・rsqrt 76 ビット）。
    spec が厳密に使う定数なので ゲートも幅を落とさず持つ（K1）。
  - **窓つき加算の固定 G は 根のテープでは足りない**: Estrin の節点の値は t の多項式で、その根が定義域の内側にあれば
    節点の相殺は 非有界。exp / log / sin / cos は根が外（格子上の最大相殺 0.3 / 0.8 / 0.2 / 0.5 bit）で G=4 が根拠つき
    になった一方、sqrt_p1 / rsqrt_p1 は根が内側（f32 1+2 個・f64 4+4 個、格子上 19〜25 bit）、rsqrt_p0 は根が無くても
    f64 で 4.4 bit > G。直し = **窓を下へ Dext = W + extra + 3 − G 桁伸ばす**（extra = 係数の W を超える幅）。正しさ:
    小さい側の LSB が窓に入れば何も落ちず厳密（相殺の深さは無関係）、落ちるのは先頭位置の差 ≥ 2 のときだけ
    （G+Dext ≥ li_S − li_U + 1、最大 W+extra+1）で相殺 ≤ 1 bit。ホスト監視も「落とした ∧ 先頭 < W」に正確化した
    （旧「常に先頭 ≥ W」は 厳密なのに鳴る偽陽性があり得た）。代金は 関数全体で f32 sqrt +25% / rsqrt +33%、f64 +23% / +25%。
  - log の尾 Ex·ln2 + log1p(t) は |log1p(t)| < 0.347 < ln2/2 なので Ex ≠ 0 のとき相殺 ≤ 1 bit、固定 G=4 で証明つき。

**新しい規律**:
- **R4 ゲート側だけの境界には単体テスト**: spec と bit 一致する差分テストの敵対例は「spec が場合分けする境界」から
  作る。ゲート実装が自分の境界（窓の底・sticky の切り替え・二段バレルの継ぎ目）を持つとき、その境界は差分テストに
  現れない。expm1 窓のバグ 2 件（借りの消失・P<0 で落ちた量が 1 単位を超える）は 535 点の差分テストを通り、
  境界値を固定した単体テストが見つけた。
- **R5 sticky の前提を数える**: sticky 桁 ±1 は「落ちた量の大きさ < 1 単位」でだけ正しい。落ちるものが 2 つ
  （P の尾と −1）あるとき和は 1 単位を超えうる。直しは「窓底に −1 桁を余分に置いて余りを (unit−1) − d に折り返す」。
- **主張の式は掃引で検算する**: hi の ulp 上界を lo と同じ式にしていた（正しくは 1 + 3·2^(Wout−e)）。f32 総当たりが
  14 件の「主張超え」で式の誤りを見つけた（値は 1 件も間違っていない）。訂正後は記録した sup で再判定（再掃引なし）。

## 二次代数の還元 = スカラー関数 + 双線形（`../total-arith-cuda/hyper_quad.py`・research・2026-09-02）

Cayley–Dickson の配線（M = 2^k）は二次代数: x = a·e0 + v（v ⊥ e0）で **v² = −|v|²·e0**（`_mul` で M=1..32 を検算、仮定でない）。
実係数の解析関数 f は f(a + v) = Re f(a + i|v|) + (v/|v|)·Im f(a + i|v|) なので、M×M の行列関数（expm/logm/funm）は要らず、
**rsqrt 1 回 + スカラー関数 3〜4 本 + 乗算 (3M+2) 回**に落ちる（n2 = |v|²・r = rsqrt(n2)（n2=0→0 = a/0=0 規約）・
u = v·r・s = n2·r・複素スカラー f(a+is) を exp/expm1/log/sqrt/rsqrt/sin/cos で組む・Re·e0 + u·Im）。
上の表の部品で数えると:

| M | 還元 exp（乗算 / 加算 / スカラー関数） | 行列 expm（積和・解析 (6+s+⅓)M³, s=0） | ゲート f64 概算: 還元 / 行列 |
|---|---|---|---|
| 2 | 6 / 0 / 4 | 51 | 3.8 M / 2.9 M（行列が ×0.8） |
| 4 | 12 / 2 / 4 | 405 | 4.0 M / 23 M（×5.7） |
| 8 | 24 / 6 / 4 | 3,243 | 4.5 M / 185 M（×41） |
| 16 | 48 / 14 / 4 | 25,941 | 5.5 M / 1,480 M（×270） |
| 32 | 96 / 30 / 4 | 207,531 | 7.5 M / 11,800 M（×1,600） |

概算の単価: rsqrt = 1,536 k（上の f64 実測）、exp / sin / cos ≈ 687 k（f64 exp 全経路の実測。sin/cos はゲート未実装で
exp と同程度と仮定 — 縮小が重い分だけ実際は上）、乗算 ≈ 33 k（Wallace 26 k + canonicalize + norm）、加算 ≈ 24 k
（窓つき加算 + canonicalize×2 + norm）、積和 ≈ 57 k。行列側は expm の Padé 13（s = 0）の下界で、funm/logm はもっと高い。
**交差点は M = 2 と 4 の間**（複素数: 2×2 の行列関数はスカラー関数 4 本と同程度で、rsqrt の実測を入れると行列側が
2 割安い）、四元数から上は M³ で還元が勝つ。（初稿は rsqrt を exp と同じ 687 k と仮定して「M = 2 で互角」としていた。
`gate_logroot.py` の実測で置き換えた。）

検証（両 backend PASS）: 行列経路と 9 関数 × M=1,2,4,8,16 × 12 点で最悪相対差 2.6e-15〜7.2e-15・SING/CPLX 判定一致、
恒等式 7 本 ≤ 1e-15、|v|→0 の連続性（単調収束・v=0 で厳密・旗不変）、全域性（NaN/Inf/1e±200/0/MAX × 9 関数で NaN 0）。

得られた 2 つ:
- **零因子の log は存在する**: z = e3+e10 で行列経路は SING（L_z 特異）だが、還元は w = ½ln2 + (π/4)(e3+e10) を返し
  **exp(w) = z が還元 exp でも行列 expm でも成立**。L_z の特異性は表現の性質で、ℝ[z] ≅ ℂ の中では log がある（一意性は主張しない）。
- **順序旗は非単調関数の不確かな引数を通れない**: (A) の spec を backend にすると log/sqrt/sin は 11（界なし）で出るが、
  exp/cosh は全成分 111（符号不明）。s = n2·rsqrt(n2) が near の 11 を運び、sin(s)/cos(s) は界も符号も失う（(A) の伝播則どおり）。
  方向しか運ばない語彙の限界で、誤差の幅（0.5 ulp）を運ぶのは四値/区間層の仕事。

未着手: **atan2 は 6 関数の外**（log/pow の虚部は float64 の atan2 に旗 11 で正直に印）。次は atan テープ
（核 atan(√y)/√y を y ≤ tan²(π/8) に、単位ベクトルの半角 3 回（各 rsqrt 1 回）で還元）。
