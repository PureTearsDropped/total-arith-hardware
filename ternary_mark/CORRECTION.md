# 訂正 — `389b34a` / `54afb67` の主張は過大だった

> 2026-08-25。外部レビュー（ChatGPT）の指摘を受けて再現し、**指摘が正しいことを確認した**。
> 先行コミットの「加減乗除すべてで嘘 0」は**成立しない**。以下を撤回する。

## 撤回する主張

| 撤回する記述 | 実際 |
|---|---|
| 「加減乗除すべてで嘘 0（各 3000〜6000 例）」 | **反例あり**（下記） |
| 「予約桁 1 つで相殺が完全に捕まる」 | 直接の桁相殺しか捕まらない。**carry 経由を取りこぼす** |
| 「規則は一本（端のトリットが無限に続く）」 | `total_word.py` の下端は**入れ子区間**で読んでおり、規則が二重 |
| 「除算 嘘 0（3000 例）」 | 試験が真値を常に nominal に置いており、**区間意味論を検査していない** |

## 反例（再現済み）

**① carry 経由の相殺が検出されない**

```
x 末尾2桁 = [ 0, +1]
y 末尾2桁 = [-1, +1]
LSB: +1 + +1 = 0 (carry +1 上へ) / MSB: 0 + (-1) + carry = 0
⟹ 厳密な和は 0。相殺は起きているが、どの位置でも「入力2桁が直接打ち消す」形では
   ないので `(xp&yn)|(xn&yp)` は発火しない（OR 版も ripple 版も印 = 0）
```

**② LE + LE で健全性が破れる（こちらが重大）**

```
u 末尾 = [0, +1]   (上端を主張 = LE)
v 末尾 = [0, +1]   (上端を主張 = LE)
両方が上端 ⟹ 和も上端であるべき
和の末尾 = [1, 0, 0]、予約桁 = 0  ⟹ **「厳密」と主張 = 嘘**
```

`+1 + +1 = 0 carry 1` で最下位が 0 になり、padding 規則では 0 = 厳密を意味するため。

## なぜテストが見逃したか（これが本体）

- `cancel_carry_down.py` の「出力 ≥ 真の和」は**空虚**。印は加算しかしないので常に成立する
- `down_ripple.py` は **OR 版と ripple 版が同じ判定式**を使う比較なので循環。両方が同じ
  取りこぼしをしても一致は成立する
- 決定的: `uniform_digit_add.py` は `GE+LE → exact（嘘）` と `LE+LE → 不定` を**実際に
  検出していた**。しかし後者を「展開深さ 24 の打ち切り artifact」と決めつけて流した。
  **あれが本物の反例だった**
- 機構を差し替えたとき（padding 意味論 → 桁相殺の印）に**テストの基準も一緒に差し替えた**
  ため、判別力が消えた。真値を「padding 込みの値」でなく「桁列の厳密和」に取ったのが誤り

## その他の指摘（いずれも正しい）

- `add_ripple` は入力長 n に対し n+1 桁を返す。**演算ごとに 1 桁増える**ので、固定幅
  1 予約桁での閉包性は未証明
- `div_as_program.py` は `truth = 1/av` と nominal に固定。`B(av,'upper')` のラベルを
  ランダムに変えても、真の入力が av と異なる場合を一度も試していない
- README と `total_word.py` で意味論が二重（前者は「端トリット無限延長」、後者は
  「入れ子区間」）。`total_word.py` 自身が「上下は完全な対称ではない」と訂正している

## 残るもの

- **表現の方向性**（不確かさを別フラグでなく数の表現空間に押し込む）は生きている
- `total_word.py` の「三値列そのものを区間表現にする」下端は、単に印を桁に隠すより強い
- 上端で「何桁ぶん越えたか」を残して有限/半無限の溢れを区別する発想も生きている
- **「4 値目 `?` は要らないかもしれない」までは言える。「予約桁 1 つで閉じた」は言えない**

## 次にやること（レビューの提案を採用）

1. README を `total_word.py` の現在の意味論に統一する（どちらか一方を選ぶ）
2. carry / compressor 内部を含む「相殺」の**定義を形式化**する
3. `Word × Word → Word` の加算を 1 個実装する（固定幅）
4. 入力区間の全端点・小幅全列挙で `I(x) + I(y) ⊆ I(add(x,y))` を検査する
5. その出力をもう一度加算して、**固定幅で閉じる**ことを検査する
6. reciprocal は nominal でなく**入力区間内の複数の真値**をオラクルにする

合成・FPGA はこの後。検証器が仮説と同じ前提を置いている間は、実機に持っていっても
同じ穴を再現するだけになる。
